import type { Project, RenderVideoUserProjectSettings } from '@revideo/core';
declare global {
    interface Window {
        onRenderComplete: () => void;
        onRenderFailed: (msg: string) => void;
    }
}
/**
 * Render the project.
 */
export declare const render: (project: Project, workerId: number, totalNumOfWorkers: number, hiddenFolderId: string, overwriteRenderSettings: RenderVideoUserProjectSettings) => Promise<void>;
//# sourceMappingURL=render.d.ts.map